<Window x:Class="NisanWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterScreen"
        Title="MainWindow" Height="600" Width="800" Closing="Window_Closing">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Column="0" LastChildFill="True">
            <DockPanel DockPanel.Dock="Left" LastChildFill="True">
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                    <Button Content="" Style="{StaticResource MetroButtonStyle}" />
                </StackPanel>
                <DockPanel />
            </DockPanel>
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                    <Button Command="{Binding CreateOrderCommand}" Content="" Style="{StaticResource MetroButtonStyle}" />
                    <Button Content="" Style="{StaticResource MetroButtonStyle}" />
                    <Button Content="" Style="{StaticResource MetroButtonStyle}" />
                    <Button Width="24" Height="24" BorderThickness="0" BorderBrush="Transparent" Background="{StaticResource KnifeIcon}" />
                    <Button Content="" Style="{StaticResource MetroButtonStyle}" />
                    <Button Content="" Style="{StaticResource MetroButtonStyle}" />
                </StackPanel>
                <DockPanel DockPanel.Dock="Bottom">
                    <WrapPanel DockPanel.Dock="Right">
                        <TextBlock Text="Total " />
                        <TextBlock Text="{Binding totalSales}" />
                    </WrapPanel>
                    <WrapPanel>
                        <TextBlock Text="{Binding Orders.Count}" />
                        <TextBlock Text=" items found" />
                    </WrapPanel>
                </DockPanel>
                <ScrollViewer VerticalScrollBarVisibility="Auto"  HorizontalScrollBarVisibility="Disabled">
                    <ItemsControl ItemsSource="{Binding Orders}" />
                </ScrollViewer>
            </DockPanel>
        </DockPanel>

        <DockPanel Grid.Column="1" LastChildFill="True">
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="" Style="{StaticResource MetroButtonStyle}" />
                <Button Content="" Style="{StaticResource MetroButtonStyle}" />
                <Button Content="" Style="{StaticResource MetroButtonStyle}" />
                <Button Content="" Style="{StaticResource MetroButtonStyle}" />
            </StackPanel>
            <WrapPanel></WrapPanel>
        </DockPanel>

    </Grid>
</Window>
